#include<stdio.h>
void main()
{
int num,rem=0,cub=0;sum=0,temp;
printf("enter the num:");
scanf("%d",&n);
while(num!=0)
{
rem=num%10;
cub=pow(rem,3);
sum=sum+cube;num=num/10;
if(num==temp)

printf("The num is amstrong",num);
else
printf("The num is not amstrong",num);
}
}
